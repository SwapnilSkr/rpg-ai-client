org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true
# Flutter 3.47 / AGP 9 default. firebase_core 4.14+ and firebase_auth 6.6+
# only apply kotlin-android when this is false; keep it true so they use
# AGP's built-in Kotlin. google_sign_in_android must be >= 7.2.12 for the
# same reason.
android.builtInKotlin=true
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
